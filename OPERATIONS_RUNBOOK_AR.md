# دليل تشغيل منصة El Hawary Careers

## الحالة الحالية

الموقع منشور على Firebase Hosting، وقاعدة البيانات تستخدم Firestore، والمصادقة تستخدم Firebase Authentication. النشر الآلي يعمل من GitHub Actions بعد نجاح فحوصات ESLint وTypeScript وVite build. تعديل الوظائف والطلبات والمحتوى ورقم واتساب يتم من لوحة الإدارة مباشرة.

## دورة النشر

يتم رفع تعديلات الكود إلى فرع `launch-readiness`. يبدأ GitHub Actions تلقائياً بتثبيت الاعتمادات، وتشغيل فحوصات الجودة، وبناء نسخة الإنتاج، ثم نشر Hosting وقواعد Firestore. لا يتم النشر إذا فشل أحد الفحوصات.

## أمن الطلبات

نموذج التقديم يطبّع المدخلات ويضع حدوداً للطول، ويلزم موافقة الخصوصية، ويستخدم معرّفاً حتمياً لمنع تكرار التقديم لنفس الوظيفة والبريد. قواعد Firestore تمنع إنشاء طلب غير مرتبط بوظيفة نشطة، وتمنع الكتابة فوق المعرّف أو تغيير بيانات الطلب بعد الإنشاء. يسمح للمدير وموظف HR بتحديث الحالة فقط.

## سجل التدقيق

تغييرات حالة الطلبات، منح وسحب صلاحيات HR، وتغيير رقم واتساب تُسجّل في مجموعة `auditLogs`. القراءة متاحة للمدير فقط، ولا يمكن تعديل أو حذف السجلات من العميل.

## الإشعارات البريدية

النسخة الحالية لا ترسل بريداً من المتصفح، لأن وضع مفتاح خدمة البريد داخل الواجهة سيعرّضه للكشف وإساءة الاستخدام. المسار الآمن لإضافة الإشعارات هو Cloud Functions أو خدمة بريد transactional عبر خادم وسيط، مع استخدام أسرار GitHub/Firebase وعدم وضعها في React. يجب تفعيل هذه الميزة بعد تحديد مزود البريد ومفتاحه وميزانية التشغيل المناسبة.

## النسخ الاحتياطي

لا توجد عملية تصدير تلقائية مفعّلة حالياً، لأن التصدير المجدول من Firestore يحتاج مشروع Google Cloud ومخزناً سحابياً وصلاحيات خدمة مخصصة. يجب إعداد Cloud Scheduler/Firestore export إلى Cloud Storage على مشروع منفصل أو حساب تخزين مخصص، مع اختبار الاستعادة قبل اعتبار النسخ الاحتياطي مكتملاً.

## التشغيل اليومي

يفحص المدير الطلبات الجديدة، يحدّث حالتها، ويتابع إرسال السيرة الذاتية عبر واتساب. يراجع الوظائف النشطة أسبوعياً، ويتأكد من صحة رقم واتساب والبريد، ويستخدم تصدير CSV من لوحة الإدارة عند الحاجة إلى تقرير للموارد البشرية.

## إجراءات الطوارئ

عند ظهور نسخة غير صحيحة، يمكن إيقاف النشر من GitHub Actions أو الرجوع إلى commit سابق. عند الاشتباه في كشف رمز Firebase، يجب إلغاء الرمز فوراً وإنشاء بديل وتحديث GitHub Secret. عند حذف بيانات بالخطأ، لا يُفترض وجود استعادة مضمونة قبل إعداد Cloud Storage export واختباره.

## التوسع إلى تطبيق جوال

تظل الوظائف والطلبات والمحتوى والأدوار في Firestore، ويمكن بناء تطبيق جوال لاحقاً باستخدام نفس المصادقة والخدمات. يجب عدم منح التطبيق صلاحيات مدير، ويجب إبقاء عمليات الإدارة مقيدة بقواعد Firestore وclaims المناسبة.
