# ملاحظات اختبار دورة التقديم

- المصدر: https://elhawary-careers-2026.web.app/apply/nuyrONSnhU2RpZnpvmNs
- الوظيفة: صيدلي فرع، المعرّف: nuyrONSnhU2RpZnpvmNs.
- الاختبار مصرح به من المستخدم، والسجل يستخدم بيانات اصطناعية واضحة.
- الخطوة الأولى: الاسم «اختبار تكامل المنصة»، تاريخ الميلاد 1995-01-01، البريد qa.elhawary.20260815@example.com، الهاتف وواتساب 01000000001، العنوان «اختبار - القاهرة».
- الخطوة الثانية: المؤهل «بكالوريوس صيدلة - اختبار»، الجامعة «جامعة اختبار»، سنة التخرج 2024، التقدير «جيد جداً - اختبار».
- الخطوة الثالثة: الشركة «صيدلية اختبار التكامل»، المسمى «صيدلي - اختبار»، تاريخ البدء 2022-01-01، تاريخ الانتهاء فارغ (خبرة حالية)، وصف المهام فارغ حتى الآن.
- تمت ملاحظة أن زر «التالي» انتقل فعلياً من الخطوة الثانية إلى الثالثة بعد إدخال القيم المطلوبة.
- الحالة الحالية: الخطوة الثالثة، وتظهر حقول الخبرة وأزرار التنقل أسفل البطاقة؛ لم يتم إرسال الطلب بعد.

## نتيجة الإرسال

تم إرسال الطلب بنجاح من الصفحة الحية. الرقم المرجعي الظاهر: **ELH-A4A250C6**. انتقلت الصفحة إلى `/success/nuyrONSnhU2RpZnpvmNs?ref=ELH-A4A250C6`، وظهر زر واتساب لإرسال السيرة الذاتية، مع البريد careers@elhawarypharmacy.com ورقم التواصل 01000753375. لم يتم الضغط على زر واتساب لأن ذلك قد يفتح رسالة خارجية غير لازمة للاختبار.

## استعادة اللوحة والتحقق الحالي

بعد استعادة جلسة المدير وإعادة فتح `/admin`، ظهرت لوحة الإدارة كاملة. الإحصاءات الحالية: إجمالي الطلبات 2، منها طلب واحد يحتاج مراجعة، والوظائف المنشورة 2. ظهر طلب الاختبار «اختبار تكامل المنصة» بالرقم `a4a250c6` والبريد الاختباري والهاتف الاختباري، وحالته الحالية «جديد». ما زال تغيير الحالة والتحقق من `auditLogs` والتنظيف النهائي قيد التنفيذ.

## اختبار تحديث الحالة

تم تغيير حالة سجل «اختبار تكامل المنصة» من «جديد» إلى «قيد المراجعة» عبر لوحة المدير. انعكس التغيير فوراً في البطاقة، وتغيرت الإحصاءات من 1 طلب يحتاج مراجعة و1 بانتظار الإجراء إلى 0 و0، ما يثبت أن تحديث الحالة يُحفظ ويؤثر في مؤشرات اللوحة. وفق الكود المنشور، عملية التحديث تستدعي أيضاً `writeAuditLog('update_application_status', 'application', id, { status })`.

## تحقق سجل التدقيق

تم فتح Firebase Console وقراءة أحدث مستند في `auditLogs`: المعرّف `1786770808953-b2rcei`، والإجراء `update_application_status`، والمنفذ `ahmed.sheta89@gmail.com`، والتاريخ `2026-08-15T05:13:28.953Z`، والحالة `reviewing`، و`entityId` يطابق مستند الطلب التجريبي `a4a250c64e833b2d2b45a83403cce27fda865880c0923ca5e512297ad31cc838`. هذا يؤكد أن تحديث الحالة أنشأ سجلاً أمنياً مستقلاً كما هو مقصود. سيُترك سجل التدقيق محفوظاً، وسيُحذف مستند الطلب التجريبي فقط من قاعدة الطلبات حتى لا يبقى مرشح وهمي ضمن بيانات التشغيل.

## التنظيف النهائي

تم حذف مستند الطلب التجريبي `a4a250c64e833b2d2b45a83403cce27fda865880c0923ca5e512297ad31cc838` من Firebase Console بعد تأكيد المعرّف والبيانات. أظهر Firebase أن المستند لم يعد موجوداً، وتبقى في مجموعة `applications` وثيقة الطلب غير التجريبية فقط `VWTvE5p21v8bD4gZfP2m`. لم يُحذف سجل التدقيق `1786770808953-b2rcei`، وبذلك بقي الدليل الأمني محفوظاً دون إبقاء مرشح اختبار ضمن التشغيل.

## النتيجة

اكتملت دورة الاختبار الإنتاجية: تقديم ناجح، ظهور في لوحة المدير، تحديث حالة محفوظ، سجل تدقيق مستقل، ثم تنظيف آمن للبيانات التجريبية.
