# دليل ربط منصة El Hawary Careers مع Google Sheets

## القرار المعماري

يبقى **Firestore هو المصدر الأساسي والآمن لطلبات التوظيف**، بينما يستخدم Google Sheets كنسخة تشغيلية وتحليلية اختيارية. لا يتم حذف الطلبات من Firestore عند المزامنة، ولا تعتمد المنصة على Google Sheets لاستقبال الطلبات أو تشغيل نموذج التقديم.

المزامنة متاحة للمدير فقط من تبويب **الطلبات** داخل لوحة الإدارة، وتعمل عند الطلب. يتم استخدام معرّف الطلب كمعرّف ثابت للصف؛ لذلك يؤدي تكرار المزامنة إلى تحديث الصف الموجود بدلاً من إنشاء صف مكرر.

> لا تضع مفتاح حساب خدمة Google أو ملف JSON سري داخل React أو GitHub أو متغيرات Vite العامة. التكامل الحالي يستخدم OAuth مؤقتاً من جلسة المدير، ولا يخزن رمز الوصول في Firestore.

## ما الذي يتم تصديره؟

يتم تصدير الأعمدة التشغيلية اللازمة لمراجعة الطلبات: معرّف الطلب، تاريخ التقديم، الحالة، معرّف الوظيفة، المسمى الوظيفي، اسم المتقدم، البريد الإلكتروني، الهاتف، واتساب، العنوان، المؤهل، الخبرات، المهارات، حالة السيرة الذاتية، ووقت آخر مزامنة.

ينبغي مشاركة ملف Sheets مع الأشخاص المسؤولين فقط، وتفعيل المصادقة الثنائية لحسابات Google التي تصل إليه. لا تستخدم رابط مشاركة عام، ولا تضف بيانات بنكية أو كلمات مرور أو أي بيانات غير ضرورية للموارد البشرية.

## إعداد Google Cloud

1. افتح [Google Cloud Console](https://console.cloud.google.com/) وأنشئ مشروعاً منفصلاً أو استخدم مشروعاً إدارياً مناسباً.
2. فعّل **Google Sheets API** من قسم APIs & Services → Library.
3. افتح APIs & Services → Credentials، ثم أنشئ OAuth Client ID من نوع **Web application**.
4. أضف نطاق الموقع الحي ضمن Authorized JavaScript origins:
   `https://elhawary-careers-2026.web.app`
5. إذا استخدمت نطاقاً مخصصاً لاحقاً، أضفه أيضاً بصيغته `https://domain.example`.
6. لا تضف Client Secret إلى الواجهة ولا إلى `.env` العام؛ تحتاج الواجهة إلى Client ID فقط، وهو ليس سراً بحد ذاته.

## إعداد ملف Sheets

1. أنشئ ملف Google Sheets خاصاً بالموارد البشرية.
2. انسخ `spreadsheetId` من الرابط، وهو الجزء الواقع بين `/d/` و`/edit`.
3. اترك اسم الورقة `Applications` أو أنشئ ورقة بالاسم الذي ستكتبه في لوحة الإدارة.
4. شارك الملف مع حساب المدير الذي سيمنح OAuth من اللوحة، وحدد صلاحية مناسبة مثل Editor.
5. لا تجعل الملف Anyone with the link.

## إعداد المنصة

1. افتح [لوحة الإدارة](https://elhawary-careers-2026.web.app/admin) بحساب المدير.
2. انتقل إلى تبويب **الطلبات**.
3. في بطاقة Google Sheets أدخل OAuth Client ID وSpreadsheet ID واسم ورقة العمل.
4. اضغط **حفظ إعدادات Sheets**.
5. اضغط **مزامنة الطلبات الآن**.
6. وافق في نافذة Google على صلاحية إدارة جداول البيانات عند أول استخدام.
7. افتح ملف Sheets وتحقق من وجود صف العناوين ثم صفوف الطلبات.

## التشغيل الآمن

يتم منع زر المزامنة أثناء التنفيذ لمنع النقر المكرر. يتم تسجيل إعدادات التكامل وعمليات المزامنة في `auditLogs` دون تسجيل رمز OAuth. Firestore يظل النسخة المرجعية، ويمكن الاستمرار باستخدام تصدير CSV والنسخة الاحتياطية JSON كبدائل مجانية.

تعمل Google Sheets API بحصص طلبات لكل دقيقة، ولذلك فإن مزامنة دفعة واحدة من لوحة المدير مناسبة للاستخدام الأولي ولا تمثل ضغطاً متوقعاً. عند التوسع إلى مزامنة تلقائية أو عدد كبير من الطلبات، يجب إضافة خدمة خلفية مصادق عليها مع معالجة إعادة المحاولة وقيود الوصول؛ لا ينبغي وضع حساب خدمة داخل استضافة Firebase الثابتة.

## استكشاف الأخطاء

إذا ظهر أن Client ID غير صحيح، فتأكد من نهايته `.apps.googleusercontent.com` ومن أن نوعه Web application. إذا رفضت Google الطلب، تأكد من تفعيل Sheets API وإضافة أصل الموقع الحي كما هو دون مسار `/admin`. إذا ظهر خطأ صلاحية على الملف، شارك جدول Sheets مع حساب Google المستخدم في نافذة OAuth. إذا لم تكن الورقة موجودة بالاسم المدخل، أنشئها أو غيّر الاسم في إعدادات اللوحة.

## حدود الخطة المجانية

لا تحتاج المزامنة اليدوية عند الطلب إلى Firebase Storage أو Cloud Functions أو Cloud Scheduler. أما إرسال كل طلب تلقائياً إلى Google Sheets في الخلفية، أو إرسال البريد تلقائياً، أو النسخ المجدول، فيحتاج طبقة خادم أو Apps Script مضبوطاً بعناية؛ لذلك لا يتم ادعاء أن هذه الوظائف تعمل تلقائياً في النسخة المجانية الحالية.

## المراجع الرسمية

- [Google Sheets API concepts](https://developers.google.com/workspace/sheets/api/guides/concepts)
- [Google Sheets API usage limits](https://developers.google.com/workspace/sheets/api/limits)
- [Google Workspace credentials](https://developers.google.com/workspace/guides/create-credentials)
