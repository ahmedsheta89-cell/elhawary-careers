# مبادئ التشغيل الاحترافية لمنصة El Hawary Careers

## مبدأ التنفيذ من البداية إلى النهاية

عند طلب تنفيذ ميزة أو تكامل للمنصة، يجب التعامل معها كمسار متكامل من البداية إلى النهاية، وليس كسلسلة خطوات منفصلة ينتظر فيها الوكيل إعادة شرح الهدف. يشمل ذلك مراجعة الحالة الحالية، تجهيز الخدمات والملفات، تنفيذ الإعدادات الممكنة، اختبار التكامل فعلياً، التحقق من الأمان وعدم التكرار، توثيق النتيجة، ثم تسليم الروابط والبيانات النهائية.

## قاعدة الصلاحيات

يُستخدم كل وصول متاح وآمن للمشروع والمتصفح والتكاملات الموثّقة لإنجاز المطلوب دون تجزئة غير ضرورية. لا يجوز طلب كلمة مرور أو رمز تحقق أو بيانات سرية من المستخدم داخل المحادثة، ولا يجوز تخزين الأسرار في المستودع أو Firestore أو ملفات الواجهة. إذا أوقفت حماية خارجية خطوة لا يمكن تنفيذها دون مصادقة بشرية، يجب تحديد هذه الخطوة بدقة مع إبقاء كل الأعمال الأخرى مكتملة.

## معيار التسليم

لا تُعلن الميزة مكتملة إلا بعد اختبارها من مسار الاستخدام الفعلي، وتوثيق ما تم تنفيذه وما بقي، وعدم الادعاء بوجود مزامنة تلقائية أو خدمة مدفوعة إذا كان التنفيذ المجاني المتاح يقتصر على المزامنة اليدوية.

## قرار Google Sheets

Firestore هو المصدر الأساسي لطلبات التوظيف، وGoogle Sheets نسخة تشغيلية اختيارية. يجب استخدام OAuth من المتصفح بأقل صلاحيات عملية، وعدم استخدام Service Account أو أسرار ثابتة في تطبيق React. ملف Sheets المخصص للتوظيف يجب أن يحتوي على ورقة `Applications` مستقلة عن أي أوراق أعمال أخرى، مع منع تكرار الطلبات اعتماداً على معرّف الطلب.

## تاريخ التوثيق

تم تثبيت هذه المبادئ في 2026-08-16 ضمن استكمال ربط Google Sheets لمنصة El Hawary Careers.
