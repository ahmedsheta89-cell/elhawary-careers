# نتائج بحث ربط Google Sheets — 16 أغسطس 2026

## مصادر رسمية

1. [Google Sheets API overview](https://developers.google.com/workspace/sheets/api/guides/concepts)
   - Google Sheets API واجهة RESTful لقراءة بيانات جداول البيانات وتعديلها.
   - تدعم إنشاء جداول البيانات، قراءة وكتابة قيم الخلايا، تعديل تنسيق الجدول، وإدارة أوراق البيانات المرتبطة.
   - لكل جدول `spreadsheetId` ثابت يمكن استخراجه من رابط الجدول.
   - يمكن تحديد الخلايا باستخدام ترميز A1 مثل `Sheet1!A1:B2`.
   - الصفحة محدثة في 23 أبريل 2026.

2. [Google Sheets API usage limits](https://developers.google.com/workspace/sheets/api/limits)
   - واجهة Sheets API تعمل بحصص طلبات لكل دقيقة ويتم تجديد الحصة كل دقيقة.
   - نتيجة البحث الرسمية تعرض مثالاً بحد 300 طلب قراءة لكل دقيقة على مستوى المشروع؛ يجب الرجوع للصفحة عند التنفيذ لتثبيت القيم الحالية.

3. [Google Workspace create credentials](https://developers.google.com/workspace/guides/create-credentials)
   - مسار الاعتماد الرسمي يشرح إعداد بيانات الاعتماد للوصول إلى ملفات Workspace، بما فيها استخدام حساب خدمة ومشاركة ملف Sheets مع بريد حساب الخدمة.

## الاستنتاج المعماري

- Firestore يبقى المصدر الأساسي للطلبات، وGoogle Sheets يكون نسخة تشغيلية/تحليلية أو تصديراً إدارياً، وليس قاعدة البيانات الوحيدة.
- لا يجوز وضع مفتاح خدمة Google داخل واجهة React أو مستودع GitHub أو متغيرات Vite العامة؛ يجب أن يتم الاستدعاء من خادم وسيط أو عبر تصدير يدوي مصادق عليه.
- الخيار المجاني الأقل خطراً للمرحلة الأولى هو زر «تصدير إلى Google Sheets» من لوحة المدير، بتصدير CSV أو Google Sheet يدوياً عبر Google Workspace. الخيار الآلي الكامل يحتاج خدمة خلفية/حساب خدمة أو Apps Script مضبوطاً بعناية.
- يجب تقليل الحقول المصدّرة، خصوصاً بيانات التواصل، وتقييد صلاحية الوصول للجدول، وتسجيل عمليات التصدير في auditLogs.
- يفضل استخدام append أو upsert بمعرّف الطلب لمنع إنشاء صفوف مكررة، مع تخزين `applicationId` في عمود مخصص.

## تفاصيل الحصص المؤكدة

صفحة [Usage limits](https://developers.google.com/workspace/sheets/api/limits) الرسمية تذكر أن الطلبات تُحتسب لكل دقيقة، وأن حد القراءة هو 300 طلباً في الدقيقة لكل مشروع و60 طلباً في الدقيقة لكل مستخدم ولكل مشروع، وحد الكتابة هو 300 طلباً في الدقيقة لكل مشروع و60 طلباً في الدقيقة لكل مستخدم ولكل مشروع. كما توصي بحجم حمولة أقصى تقريبي يبلغ 2 ميغابايت لتسريع الطلبات، وباستخدام exponential backoff عند ظهور HTTP 429. طلبات Sheets الذرية لا تُطبَّق جزئياً إذا كان الطلب غير صالح، والمهلة القصوى لمعالجة طلب واحد 180 ثانية. لا يوجد حد يومي طالما بقي التطبيق ضمن حصص الدقيقة الحالية.

القرار: تصدير دفعة واحدة من لوحة المدير لا يمثل ضغطاً على الحصة. يجب إضافة منع للنقر المكرر، وإظهار حالة التصدير، وتسجيل آخر تصدير، وإعادة المحاولة المحدودة مع backoff عند الحاجة.

## المراجع

[1]: https://developers.google.com/workspace/sheets/api/guides/concepts "Google Sheets API Overview"
[2]: https://developers.google.com/workspace/sheets/api/limits "Google Sheets API Usage Limits"
[3]: https://developers.google.com/workspace/guides/create-credentials "Google Workspace Create Credentials"

## المصادقة والامتيازات

صفحة [Create credentials](https://developers.google.com/workspace/guides/create-credentials) تميّز بين مفتاح API للبيانات العامة، وOAuth للوصول إلى بيانات المستخدم، وحساب الخدمة للوصول بحساب روبوت. أدوار IAM في Google Cloud لا تمنح وحدها إذن الوصول إلى ملفات Google Workspace؛ للوصول إلى جدول محدد يجب مشاركة الملف مباشرةً مع عنوان بريد حساب الخدمة، ومنح أقل مستوى لازم مثل Editor. أما مفتاح حساب الخدمة JSON فهو نسخة حساسة وحيدة يجب حفظها بأمان وعدم وضعها في كود الواجهة أو مستودع GitHub أو متغيرات Vite العامة.

القرار الأمني: لن نضع بيانات حساب خدمة داخل Firebase Hosting/React. إن احتجنا تصديراً آلياً كاملاً، فلابد من خادم خلفي أو Apps Script مضبوط بواجهة سرية وقائمة سماح، مع تقليل الحقول وتسجيل التصدير. وضمن متطلبات Firebase Spark المجانية الحالية، الخيار الأول الموصى به هو تصدير CSV من لوحة الإدارة ثم فتحه/استيراده في Google Sheets، أو تمكين Google Sheets API عبر موصل/خادم خارجي مصادق عليه فقط بعد موافقة المستخدم وتجهيز بيانات الاعتماد.
