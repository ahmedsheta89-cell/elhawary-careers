# خطة المراقبة الأسبوعية لمزامنة El Hawary Careers

## الغرض

هذا الإجراء يراقب صحة تكامل Google Sheets دون اعتبار Sheets مصدراً للحقيقة. يظل Firestore المصدر الأساسي للطلبات، بينما تُستخدم ورقة `Applications` كنسخة تشغيلية وتحليلية، وتُستخدم ورقة `QA_Batch_Test` لاختبار الكتابة ومنع التكرار بعيداً عن بيانات المتقدمين الحقيقية.

المراقب يفحص تطابق الأعمدة الخمسة عشر، عدد الصفوف، التكرار في معرّفات الطلبات، اكتمال معرّفات QA، صحة حقول `آخر مزامنة`، عمر آخر مزامنة، وزمن قراءة النطاقات. وعند تمرير الخيار `--run-batch-test` يشغّل اختباراً معزولاً من جولتين على تبويب QA فقط.

## نتيجة تشغيل فعلية

تم تشغيل المراقب في `2026-08-16T04:19:53Z` على ملف `El Hawary Careers Applications`.

| المؤشر | النتيجة | الحكم |
|---|---:|---|
| تطابق مخطط `Applications` | صحيح | ناجح |
| تطابق مخطط `QA_Batch_Test` | صحيح | ناجح |
| صفوف بيانات الإنتاج المقروءة | 1 | لا مشكلة بنيوية |
| معرّفات الإنتاج المكررة | 0 | ناجح |
| عدد كل معرّف QA | 1 | ناجح |
| طوابع مزامنة غير صالحة | 0 | ناجح |
| عمر أحدث مزامنة | 0.18 يوم | غير متقادم وفق حد 7 أيام |
| زمن قراءة `Applications` | 556.98 مللي ثانية | طبيعي لهذا الفحص |
| زمن قراءة `QA_Batch_Test` | 381.90 مللي ثانية | طبيعي لهذا الفحص |
| نتيجة المراقب | `passed=true` | ناجح |

في إعادة التشغيل الأسبوعية الحالية كانت الجولة الأولى `inserted=0, updated=4` لأن صفوف QA كانت موجودة من الاختبار السابق، وكانت الجولة الثانية `inserted=0, updated=4`. هذا هو السلوك الصحيح عند إعادة تشغيل الاختبار: لا ينشئ صفوفاً جديدة ولا يكرر المعرّفات. أما الجولة التاريخية الأولى عند إنشاء التبويب فكانت `inserted=4, updated=0`، ثم أصبحت الجولة التالية `inserted=0, updated=4`.

يُسجّل اختبار Firestore في النتيجة كـ `skipped_due_to_authentication` بسبب رفض اعتماد Firebase CLI المحلي بـ HTTP 401. لذلك فإن هذا التشغيل يثبت سلامة طبقة Sheets ومنطق التحديث، ولا يدّعي إنشاء طلبات QA في Firestore.

## التشغيل اليدوي الأسبوعي

من جهاز موثوق تكون فيه أداة Google Workspace موثقة بالحساب الصحيح، شغّل:

```bash
cd /home/ubuntu/elhawary-careers
python3 -m py_compile scripts/weekly_sync_monitor.py
python3 scripts/weekly_sync_monitor.py --run-batch-test --max-stale-days 7
```

يكتب المراقب النتيجة المهيكلة في `weekly-sync-monitor-results.json`. تكون النتيجة التشغيلية سليمة عندما تكون قيمة `passed` مساوية لـ `true`، وتكون حالات `applicationsDuplicateIds` و`qaIssues` فارغة، وتكون `staleSync` مساوية لـ `false`. لا تُعدّل ورقة `Applications` أثناء هذا الفحص؛ التحديثات الاختبارية محصورة في `QA_Batch_Test`.

لفحص حالة المزامنة دون تنفيذ اختبار الكتابة في QA استخدم:

```bash
python3 scripts/weekly_sync_monitor.py --max-stale-days 7
```

## جدولة مجانية عملية

| الخيار | كيف يعمل | التكلفة المباشرة | القيود | القرار |
|---|---|---|---|---|
| تشغيل أسبوعي على جهاز Linux أو خادم يملكه المشروع | يستدعي `weekly_sync_monitor.py` من `cron`، ويحتاج أن تكون أداة Google Workspace موثقة | لا توجد خدمة إضافية | يتوقف إذا كان الجهاز مغلقاً أو انتهت جلسة الاعتماد | الخيار المجاني الأبسط |
| تشغيل أسبوعي داخل مهمة مؤتمتة مرتبطة بالحساب | ينشئ تقريراً أسبوعياً ويعرضه داخل مساحة العمل عند نجاح الصلاحية | لا توجد بنية خادم إضافية | يعتمد على صلاحية إنشاء الجدولة واستمرار موصل Google Workspace | مناسب بعد السماح بالجدولة |
| GitHub Actions أسبوعي | يشغّل الاختبار من مستودع GitHub | يعتمد على حصة GitHub | يحتاج اعتماد Google آمن داخل GitHub؛ لا نضع OAuth أو service-account في المستودع الحالي | غير مفعّل حالياً |
| عامل خلفي دائم أو Firestore trigger | يفحص أو يزامن عند كل طلب جديد | قد يحتاج بيئة وفوترة | لا يطابق شرط المجانية المضمونة الحالي | مؤجل |

لم تُنشأ جدولة خارجية في هذا التشغيل لأن فحص حالة الجدولة أعاد `403 permission_denied`. لذلك تم تسليم السكربت وتعليمات `cron` كحل قابل للتشغيل فوراً، من دون الادعاء بوجود مراقبة أسبوعية مفعلة في الخلفية.

## مثال Cron أسبوعي

لتشغيل الفحص كل يوم أحد الساعة 09:00 بتوقيت الجهاز، أضف السطر التالي إلى `crontab` على جهاز موثوق:

```cron
0 9 * * 0 cd /home/ubuntu/elhawary-careers && /usr/bin/python3 scripts/weekly_sync_monitor.py --run-batch-test --max-stale-days 7 >> logs/weekly-sync-monitor.log 2>&1
```

أنشئ مجلد السجل مرة واحدة قبل التفعيل:

```bash
mkdir -p /home/ubuntu/elhawary-careers/logs
```

يجب حفظ السجل على جهاز خاص وعدم رفعه إلى GitHub، لأن نتائج المراقبة قد تتضمن معرّفات أو بيانات تشغيلية. لا تستخدم هذا المثال على جهاز مشترك، ولا تضع رموز OAuth أو كلمات مرور في ملف cron.

## قواعد التعامل مع الفشل

إذا فشل الوصول إلى Google Sheets، سجّل حالة `access_failure` ولا تعدّل بيانات الإنتاج. إذا لم تطابق العناوين المتوقعة، أوقف الاختبار وافحص اسم الورقة أو ترتيب الأعمدة قبل أي كتابة. إذا ظهر معرّف مكرر في `Applications`، لا تحذف صفاً آلياً؛ أوقف المزامنة وراجع `applicationId` وسجل التدقيق يدوياً. إذا أصبح `staleSync=true`، شغّل المزامنة اليدوية من لوحة الإدارة بعد التأكد من جلسة المدير وOAuth.

إذا فشل حقن Firestore بسبب 401 أو 403، لا تُخفِ الفشل ولا ترفع مستوى الاختبار إلى اختبار إنتاجي. سجّل النطاق كـ `sheets_layer_only`، كما يفعل السكربت الحالي، لأن إثبات عدم التكرار في Sheets لا يثبت نجاح كتابة Firestore.

## الملفات المرتبطة

| الملف | الاستخدام |
|---|---|
| `scripts/weekly_sync_monitor.py` | المراقب الأسبوعي القابل لإعادة التشغيل |
| `weekly-sync-monitor-results.json` | آخر نتيجة مهيكلة محلية |
| `scripts/batch_sync_test.py` | اختبار QA من جولتين ومنع التكرار |
| `batch-sync-test-report-ar.md` | التقرير العربي للاختبار الدفعي |
| `FINAL_READINESS_REPORT_AR.md` | تقرير جاهزية المنصة والقيود التشغيلية |
| `/home/ubuntu/skills/elhawary-careers-operations/SKILL.md` | المهارة التشغيلية القابلة لإعادة الاستخدام |

## المراجع

[1]: https://developers.google.com/workspace/sheets/api/limits "Google Sheets API usage limits"
[2]: https://firebase.google.com/pricing "Firebase Pricing"
[3]: https://firebase.google.com/docs/firestore/quotas "Firestore quotas and limits"
