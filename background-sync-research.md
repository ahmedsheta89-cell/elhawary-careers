# تقييم Background Sync — مصادر رسمية

## النتائج المؤكدة

1. خطة Firebase المجانية Spark توفر Firestore بحدود يومية، وتوفر Hosting وAuthentication، لكن جدول التسعير الرسمي يضع Cloud Functions ضمن ميزات خطة Blaze لا Spark. المصدر: https://firebase.google.com/pricing
2. مستند Firestore الرسمي يثبت أن Cloud Functions يمكنها الاستماع إلى أحداث onCreate/onUpdate/onDelete في مستندات Firestore، ما يجعلها المسار الطبيعي للمزامنة عند وصول طلب جديد. المصدر: https://docs.cloud.google.com/firestore/native/docs/extend-with-functions
3. Google Sheets API متاح دون تكلفة إضافية للاستخدام القياسي، مع حدود 300 قراءة و300 كتابة في الدقيقة لكل مشروع، و60 قراءة/كتابة في الدقيقة لكل مستخدم/مشروع. يجب استخدام دفعات وexponential backoff عند 429. المصدر: https://developers.google.com/workspace/sheets/api/limits
4. صفحة Sheets API الحالية تذكر أن تجاوز حدود الطلبات مخطط أن يخضع للفوترة لاحقاً في 2026؛ لذلك يجب إبقاء الاستخدام منخفضاً وعدم تصميم النظام على polling كثيف.

## الاستنتاج التشغيلي

- المزامنة التلقائية عند إنشاء طلب جديد تحتاج طبقة خلفية موثوقة تحفظ أسرار OAuth/حساب خدمة وتتعامل مع إعادة المحاولة ومنع التكرار. لا ينبغي تنفيذها من متصفح المدير أو من Cloud Function على Spark فقط.
- أفضل حل مجاني عملي حالياً هو إبقاء Firestore مصدراً أساسياً واستخدام المزامنة اليدوية الحالية، مع إمكان إضافة تشغيل مجدول منخفض التكرار عبر خدمة مجانية إذا كانت متاحة فعلياً ضمن حساب المشروع، بشرط عدم الادعاء بأنه مزامنة فورية.
- الحل الحدثي الأقوى هو Firestore-triggered backend، لكنه يتطلب Blaze/الفوترة حسب صفحة Firebase الحالية، حتى لو بقيت الاستهلاكات ضمن الحصة المجانية في بعض الحالات. لذلك لا يطابق شرط «لا أي خدمة مدفوعة أو تكاليف إضافية» كضمان مطلق.
- لا يوصى باستخدام browser onSnapshot كخدمة خلفية: يتطلب بقاء صفحة مفتوحة، يعرّض OAuth للانتهاء، ويتوقف عند إغلاق المتصفح أو فقدان الجلسة.

## خيارات يجب عرضها للمستخدم

| الخيار | المفاضلة | التكلفة | تعقيد الإعداد |
|---|---|---|---|
| المزامنة اليدوية الحالية من لوحة الإدارة | الأكثر أماناً ووضوحاً، لكنها ليست تلقائية | مجاني | منخفض |
| تشغيل مجدول منخفض التكرار من خدمة خلفية مجانية | لا يحتاج بقاء المتصفح، لكنه ليس فورياً وقد يعتمد على حصة/سياسة مزود الخدمة | قد يكون مجانياً ضمن الحدود، بلا ضمان أبدي | متوسط |
| Firestore trigger خلفي عند كل طلب | فوري وقابل للتوسع ويمنع الاعتماد على المتصفح، لكنه يحتاج بيئة وظائف خلفية وإعداد فوترة Blaze وفق Firebase | ليس مضمون المجانية | متوسط/عالٍ |

## التوصية المؤقتة

لا نفعّل Background Sync فورية على المشروع الحالي ضمن شرط «مجاني بالكامل». نُبقي المزامنة اليدوية كمسار إنتاجي موثوق، ونضيف اختباراً دفعياً شاملاً وسجلّاً تشغيلياً. يمكن لاحقاً بناء عامل خلفي مستقل فقط بعد قبول المستخدم صراحةً بمتطلبات حساب الفوترة، مع وضع حدود استخدام وتنبيهات تكلفة.

## المراجع

[1]: https://firebase.google.com/pricing "Firebase Pricing"
[2]: https://docs.cloud.google.com/firestore/native/docs/extend-with-functions "Extending Firestore with Cloud Functions"
[3]: https://developers.google.com/workspace/sheets/api/limits "Google Sheets API Usage limits"

