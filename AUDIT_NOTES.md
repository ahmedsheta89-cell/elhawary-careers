# تدقيق أولي — El Hawary Careers

## الحالة الحالية

- المستودع الرئيسي يحتوي واجهة React/Vite/TypeScript لمنصة توظيف.
- توجد صفحات واجهة للصفحة الرئيسية، الوظائف، تفاصيل الوظيفة، التقديم، النجاح، من نحن، المزايا، والتواصل.
- البيانات المستخدمة في الواجهة ثابتة في `src/services/mockData.ts`.
- لا توجد في شجرة الفرع الرئيسي نقطة تشغيل `src/main.tsx` أو `src/App.tsx` أو Router فعلي، رغم أن `index.html` يشير إلى `/src/main.tsx`.
- `npm run build` يفشل قبل بناء الواجهة بسبب إعدادات TypeScript project references: `tsconfig.app.json` يشير إلى `tsconfig.node.json` الذي لا يعرّف `composite: true` ويستخدم `noEmit: true`.

## فجوات التشغيل الأساسية

1. لا يمكن إطلاق الفرع الرئيسي كما هو قبل إضافة نقطة التشغيل والراوتر وإصلاح إعدادات TypeScript.
2. نموذج التقديم في `src/features/apply/ApplyPage.tsx` لا يجمع قيم الحقول فعلياً؛ `handleSubmit` يحاكي طلب API عبر timeout ثم ينتقل إلى صفحة النجاح.
3. أزرار تسجيل الدخول والتقديم في `src/app/components/layout/header.tsx` لا تحتوي روابط أو إجراءات فعلية.
4. نموذج التواصل في `src/features/contact/ContactPage.tsx` لا يحتوي `onSubmit` ولا خدمة إرسال، وزر WhatsApp بلا رابط.
5. الخريطة في صفحة التواصل placeholder وليست خريطة فروع فعلية.
6. بيانات الاتصال والشعار والمسارات تحتوي قيماً تجريبية أو ملفات غير موجودة مثل `/assets/logo.svg`، وأرقام الهاتف/البريد تحتاج اعتماداً من صاحب المشروع.
7. الصفحة الرئيسية تحتوي أخطاء محتملة تحتاج إصلاحاً، منها مفتاح FAQ يستخدم `stat.label` بدلاً من `faq`، وبعض SVG viewBox غير صحيحة، ونص عربي/إنجليزي مختلط.
8. محتوى الصفحة يتضمن ادعاءات تشغيلية مثل أكثر من 50 فرعاً وتأمين صحي ومكافآت وشهادات؛ يجب اعتمادها أو حذفها قبل الإطلاق.

## ما هو موجود خارج الفرع الرئيسي

- يوجد فرع `el-hawary-careers-firebase-production-hardening` يحتوي ملفات Firebase وتكاملات أولية:
  - `src/lib/firebase.ts`
  - `src/services/jobsService.ts`
  - `src/services/applicationsService.ts`
  - `src/services/authService.ts`
  - `src/services/storageService.ts`
  - `firestore.rules`
  - `storage.rules`
  - `firebase.json`
- هذا الفرع لا يبدو مدموجاً مع واجهة الفرع الرئيسي؛ فروقات الفرع تُظهر حذف معظم الواجهة وإضافة طبقة الخدمات فقط، لذلك المطلوب ليس اختيار فرع واحد عشوائياً بل دمج الواجهة مع الخدمات واختبار قواعد الأمان.
- لا يوجد فيديو أو ملف وسائط أو رابط فيديو داخل شجرة المستودع أو تاريخ Git الذي تم فحصه؛ التركيز سيكون على جاهزية المنتج نفسه.

## تعريف الجاهزية المقترح

يجب أن يستطيع الزائر تصفح وظائف حقيقية، البحث والتصفية، فتح التفاصيل، إرسال طلب كامل مع CV، استلام رقم طلب، وأن يرى فريق الموارد البشرية الطلبات ويغير حالتها ويتواصل مع المتقدم. يجب أيضاً تثبيت المحتوى والهوية والبيانات الحقيقية، حماية الملفات والطلبات، تفعيل التحليلات الأساسية، وإجراء اختبار بناء واختبار رحلة كاملة قبل الإطلاق.


## ما تم تنفيذه في فرع launch-readiness

- تمت إضافة `src/main.tsx` و`src/App.tsx` وربط المسارات العامة وصفحة 404.
- تم إصلاح إعداد TypeScript ومكونات UI وأخطاء lint حتى ينجح `npm run lint` و`npm run build`.
- تمت إضافة تهيئة Firebase lazy، وخدمة الوظائف، وخدمة الطلبات، ورفع المرفقات، وملفات `.env.example` وقواعد Firestore وStorage.
- أصبح نموذج التقديم متعدد الخطوات ويحفظ الطلب ويرجع رقماً مرجعياً عند توفر Firebase، مع fallback محلي للتجربة.
- تمت إضافة لوحة إدارة محمية على `/admin` لتسجيل المدير، متابعة الطلبات، تغيير الحالات، وإنشاء الوظائف وتعديلها وإغلاقها وإعادة فتحها.
- تم توحيد الهاتف والبريد مع الإعداد المركزي وربط زر واتساب، وتنظيف النصوص التسويقية غير المترجمة.
- تم فحص `/careers` و`/jobs/1` و`/apply/1` بصرياً، وجميعها تعمل في نسخة التطوير.

## ما يزال مطلوباً قبل الإعلان العام

- تعبئة بيانات Firebase الحقيقية في `.env` وتطبيق قواعد الحماية فعلياً.
- إنشاء حساب مدير وإضافة custom claim باسم `admin=true`.
- إدخال الوظائف والمواعيد والراتب والمزايا الحقيقية، واعتماد بيانات الاتصال والهوية والشعار.
- تنفيذ اختبار إرسال حقيقي من جهاز خارجي مع ملف CV، ثم التحقق من ظهور الطلب والملف داخل حساب المدير.
- إضافة تنزيل مرفقات آمن من لوحة الإدارة، وإشعارات البريد، وسياسة الخصوصية والموافقة على معالجة البيانات.
