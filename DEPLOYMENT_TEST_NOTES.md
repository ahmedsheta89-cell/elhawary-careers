# ملاحظات اختبار النشر

- رابط Firebase Hosting: https://elhawary-careers-2026.web.app
- الصفحة الرئيسية فتحت بنجاح وعرضت واجهة صيدلية الهواري.
- صفحة `/careers` فتحت لكن عرضت رسالة تعذر تحميل الوظائف؛ السبب المرجح كان اعتماد الاستعلام على فهرس Firestore مركب أو عدم وجود وظائف منشورة. تم تعديل الاستعلام لاحقاً ليستخدم where فقط ثم يرتب النتائج محلياً.
- صفحة `/admin` فتحت وتعرض شاشة دخول الإدارة.
- Cloud Storage غير مفعّل في مشروع Firebase، لذلك يعتمد الإصدار الحالي على حفظ طلبات Firestore ومسار إرسال CV عبر WhatsApp دون رفع ملفات.

بعد إعادة البناء مع query string لتجاوز التخزين المؤقت، أصبحت صفحة `/careers?v=2` تعمل بلا خطأ وتعرض 0 وظيفة، ما يؤكد أن المشكلة السابقة كانت من النسخة المخزنة مؤقتاً أو الاستعلام القديم. لا توجد حالياً وثائق jobs منشورة في Firestore، لذلك تظهر رسالة عدم وجود وظائف مطابقة. يلزم إنشاء أول وظيفة من لوحة الإدارة أو Firebase Console قبل استقبال الطلبات.

تم فتح Firebase Console بحساب ahmedsheta89@gmail.com، وتأكدت قاعدة Firestore الافتراضية في المنطقة nam5. بدأ إنشاء مجموعة `jobs` لإضافة الإعلان الأول. القيم المعتمدة من التطبيق: category=`pharmacist`، type=`full-time`، experienceLevel=`entry` أو `mid`، والتعليم `bachelor`، مع location ككائن city/governorate/address.

تمت إضافة الوظيفة المعتمدة بنجاح عبر Firestore REST إلى `jobs/nuyrONSnhU2RpZnpvmNs`: صيدلي فرع، دوام كامل، مستوى entry، مؤهل bachelor، نشطة، تاريخ النشر الحالي، وانتهاء بعد 90 يوماً.

اختبار الموقع العام نجح: `https://elhawary-careers-2026.web.app/careers` يعرض «عرض 1 من 1 وظيفة»، والعنوان «صيدلي فرع»، القسم صيادلة، المكان جمهورية مصر العربية/جميع الفروع حسب الاحتياج، مبتدئ، دوام كامل، و3 مزايا.

اختبار صفحة التفاصيل نجح على `/jobs/nuyrONSnhU2RpZnpvmNs`: الوصف والمسؤوليات والمتطلبات والمزايا والموعد النهائي ظاهرة، وزر «قدم الآن» موجود. نموذج التقديم نجح على `/apply/nuyrONSnhU2RpZnpvmNs` ويعرض رقم واتساب إلزامياً لإرسال السيرة الذاتية لاحقاً، مع خطوات المعلومات الشخصية والمؤهلات والخبرات والتواصل.

## 2026-08-14 — CSS production fix and visual verification

تم اكتشاف أن PostCSS config كان مفقوداً، لذلك كانت نسخة الإنتاج تعرض HTML بتنسيق المتصفح الافتراضي. أُضيف `postcss.config.cjs` مع Tailwind وAutoprefixer، وأصبح حجم CSS الإنتاجي نحو 34.5 KB بدلاً من أقل من 1 KB. أُضيفت أيضاً aliases صريحة للألوان (`text-primary`, `text-secondary`, `background-alternate`, `border-default`) ونظام CSS عام للخطوط والخلفيات والتركيز.

بعد النشر على Firebase Hosting، ظهرت صفحة الوظائف بتنسيق Tailwind فعلي: شريط تنقل، Hero أزرق، حقول بحث وتصفية، وبطاقة الوظيفة المنشورة «صيدلي فرع». لوحة الإدارة تعرض وظيفة واحدة منشورة وحقولاً منسقة. يجب إجراء مراجعة نهائية على الهاتف قبل التسليم.

التحقق البصري النهائي للصفحة الرئيسية بعد النشر: اكتملت حركة Hero وظهر العنوان العربي والتباين والأزرار بوضوح. الصفحة تعرض الوظيفة المنشورة «صيدلي فرع» ضمن قسم أحدث الوظائف، مع أقسام المزايا ورحلة التوظيف والأسئلة الشائعة. تم نشر الإصدار على Firebase Hosting بعد نجاح lint وtypecheck وbuild.

## 2026-08-14 — WhatsApp settings verification

تم حفظ الرقم `0201000753375` من لوحة الإدارة المنشورة على `/admin?v=20260814-1`، وظهرت رسالة «تم حفظ رقم واتساب بنجاح.» في الواجهة. أعاد فحص Firestore العام HTTP 200 للمستند `settings/public` مع `whatsappNumber=0201000753375`. تم اختبار صفحة النجاح على `/success/test-job?ref=TEST-2026` دون إنشاء طلب حقيقي؛ رابط واتساب المنشور احتوى على `https://wa.me/0201000753375` ورسالة عربية مشفّرة تتضمن المرجع `TEST-2026`.

## 2026-08-15 — Job visibility diagnosis

أظهر Firebase Console أن وثيقة `jobs/nuyrONSnhU2RpZnpvmNs` موجودة، لكن `isActive=false`، بينما `expiryDate=2026-11-12` ما زال صالحاً. خدمة الموقع العامة تستعلم عن `isActive == true`، ولذلك ظهرت صفحة الوظائف كـ 0 من 0 وظيفة. تم طلب وتلقي تأكيد المدير لإعادة فتح الوظيفة من لوحة الإدارة.


## 2026-08-15 — Job reopened and public listing verified

تم الضغط على «إعادة فتح» لوظيفة `nuyrONSnhU2RpZnpvmNs` من لوحة الإدارة بعد تأكيد المدير. تحولت الحالة إلى «منشورة»، وأصبحت مؤشرات اللوحة تعرض وظيفة منشورة ووظيفة نشطة واحدة. صفحة `/careers?v=job-open-20260815` تعرض الآن «عرض 1 من 1 وظيفة» وبطاقة «صيدلي فرع».


صفحة الوظائف العامة بعد إعادة الفتح تعرض الوظيفة «صيدلي فرع»، النوع دوام كامل، المستوى مبتدئ، وعدد الوظائف «1 من 1».


تم فتح رابط التفاصيل `/jobs/nuyrONSnhU2RpZnpvmNs` بنجاح، وظهرت بيانات وظيفة «صيدلي فرع» وزر «قدم الآن». تم الضغط على الزر، فانتقل بنجاح إلى `/apply/nuyrONSnhU2RpZnpvmNs` وظهر نموذج التقديم متعدد الخطوات مع حقول الاسم والبريد والهاتف وواتساب. لم يتم إرسال طلب حقيقي أثناء الاختبار.


## 2026-08-15 — Header CTA links fixed

تم إصلاح `src/app/components/layout/header.tsx` لربط أزرار الشريط العلوي فعلياً عبر React Router: زر «تسجيل الدخول» يذهب إلى `/admin`، وزر «قدم الآن» يذهب إلى `/careers`، مع تطبيق الإصلاح على سطح المكتب والقائمة المحمولة. نجحت فحوصات lint وtypecheck وbuild، ثم تم النشر إلى Firebase Hosting. اختبار زر «تسجيل الدخول» من الصفحة الرئيسية انتقل إلى `/admin` بنجاح، وظهرت لوحة الإدارة للمستخدم المدير.


تم اختبار زر «قدم الآن» من الصفحة الرئيسية بعد النشر، وانتقل بنجاح إلى `/careers` حيث ظهرت وظيفة «صيدلي فرع». بذلك أصبح الزران العلويان فعّالين: «تسجيل الدخول» إلى `/admin` و«قدم الآن» إلى `/careers`.


## 2026-08-15 — Contact information correction in progress

تم تأكيد دخول المدير إلى لوحة الإدارة الحية، وظهور بطاقة «إعدادات التواصل». الرقم المطلوب تحديثه إلى `01000753375`، بينما البريد `careers@elhawarypharmacy.com` موجود بالفعل في الإعداد المركزي. يوجد طلب توظيف واحد ظاهر في اللوحة، ما يؤكد أن مسار التقديم يعمل.


فحص لوحة الإدارة أظهر أن البطاقة تعرض خيار حفظ رقم واتساب، بينما التذييل الحالي يعرض الرقم القديم `+20 123 456 7890` والبريد الصحيح `careers@elhawarypharmacy.com`. سيتم تحديث الرقم في الكود ثم حفظ الرقم المحلي `01000753375` في `settings/public` من لوحة الإدارة.


أُدخل الرقم `01000753375` بنجاح في حقل إعدادات واتساب. لم تُحفظ العملية في المحاولة الأولى بسبب تغيّر موضع الصفحة بعد الإدخال؛ سيتم تحديد زر «حفظ الرقم» من لقطة محدثة قبل النقر.


الرقم الصحيح `01000753375` ظاهر في الحقل، وتظهر بطاقة الحفظ على الشاشة. محاولة النقر بالاعتماد على لقطة قديمة لم تُنفذ بسبب تغيّر حالة الصفحة؛ سيتم أخذ لقطة جديدة ثم الضغط على زر الحفظ مباشرة.


تم حفظ الرقم `01000753375` من لوحة الإدارة، ويظهر الرقم الجديد في الحقل بعد الحفظ. سيتم الآن بناء ونشر تحديثات بيانات الاتصال المحلية، بما فيها التذييل ورابط واتساب الدولي.


نتائج تحديث بيانات الاتصال المنشورة:
- الموقع: https://elhawary-careers-2026.web.app/?v=contact-update-20260815
- الصفحة العامة تعرض في التذييل: الهاتف `01000753375` والبريد `careers@elhawarypharmacy.com`.
- صفحة النجاح: https://elhawary-careers-2026.web.app/success/test-job?ref=CONTACT-2026&v=contact-update-20260815
- صفحة النجاح تعرض بيانات الاتصال الجديدة وزر «إرسال السيرة الذاتية عبر واتساب».
- تم تشغيل lint وtypecheck وbuild بنجاح، ثم نشر الاستضافة على مشروع Firebase `elhawary-careers-2026`.

## 2026-08-15 — Password reset and HR role support

- أضيف زر «نسيت كلمة المرور؟» إلى شاشة `/admin` وربط بدالة Firebase `sendPasswordResetEmail`.
- أضيف اكتشاف custom claims: `admin=true` للمدير و`hr=true` لموظف الموارد البشرية.
- موظف HR يرى الطلبات ويستطيع تحديث الحالة فقط؛ أقسام الوظائف وإعدادات التواصل مخفية عنه.
- قواعد Firestore الجديدة تسمح لـ HR بقراءة الطلبات وتحديث `status` و`updatedAt` فقط، وتمنعه من تعديل الوظائف أو الإعدادات أو حذف الطلبات. تم تجميع القواعد ونشرها بنجاح.
- تم تشغيل `lint` و`typecheck` و`build` بنجاح ونشر نسخة الواجهة على Firebase Hosting.
- تم تسجيل الخروج من جلسة المدير والتحقق بصرياً من ظهور زر «نسيت كلمة المرور؟» في النسخة الحية. لم يتم إرسال رسالة إعادة تعيين أثناء الاختبار.
- يحتاج تفعيل HR الفعلي إلى البريد الإلكتروني لموظف الموارد البشرية لتعيين claim باسم `hr=true`.
