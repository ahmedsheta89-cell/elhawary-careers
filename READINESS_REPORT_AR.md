# تقرير جاهزية El Hawary Careers

## الحكم المختصر

المشروع **جاهز تقنياً للبناء والنشر كواجهة تجريبية/مرحلة إطلاق مضبوطة**، لكنه **ليس جاهزاً بعد للاستخدام الإنتاجي العام دون إعداد Firebase واختبار تدفق حقيقي**.

بمعنى أدق: الشيفرة تتجمع بنجاح، والمسارات والواجهة والخدمات الأساسية موجودة، لكن الجاهزية النهائية تعتمد على إعداد مشروع Firebase الفعلي، إنشاء المدير وتعيين claim، نشر القواعد، إدخال متغيرات البيئة، ثم اختبار طلب توظيف حقيقي. كما أن إشعارات البريد ليست مفعلة في النسخة الحالية؛ تحتاج إلى Cloud Function أو مسار خلفي مستقل.

## ما تم التحقق منه

| الفحص | النتيجة |
|---|---|
| `npm run lint` | ناجح، مع تحذير توافق غير مانع بين TypeScript وإصدار `@typescript-eslint`. |
| `npm run typecheck` | ناجح. |
| `npm run build` | ناجح، وتم إنشاء مجلد `dist`. |
| `npm run format:check` | ناجح. |
| مسارات الواجهة | الصفحة الرئيسية، الوظائف، التفاصيل، التقديم، النجاح، التواصل، 404، ولوحة الإدارة موجودة. |
| الحماية | قواعد Firestore وStorage تعتمد على `admin=true` للعمليات الإدارية. |
| إعدادات النشر | تمت إضافة Firebase Hosting SPA rewrite وملف `vercel.json` في commit `18f056e`. |

تحذير TypeScript الظاهر أثناء lint لا يفشل البناء، لكنه يستحسن معالجته لاحقاً بتوحيد نسخة TypeScript مع النطاق المدعوم من `@typescript-eslint`.

## ما الذي ينقص قبل الإطلاق العام؟

### 1. إعداد Firebase الفعلي

يجب إنشاء أو اختيار مشروع Firebase، وإضافة Web App، وتفعيل Email/Password في Authentication، وإنشاء Firestore وStorage. بعد ذلك تُملأ قيم `VITE_FIREBASE_*` في بيئة النشر. هذه القيم ضرورية لكي يتوقف التطبيق عن استخدام fallback المحلي ويتصل ببيانات حقيقية.

### 2. حساب المدير

أنشئ مستخدماً منفصلاً للإدارة داخل Firebase Authentication، ثم عيّن له custom claim باسم `admin: true` من Firebase Admin SDK أو من بيئة خادمية آمنة. لا يمكن اعتبار `/admin` جاهزاً قبل تنفيذ ذلك؛ فالمستخدم قد يستطيع تسجيل الدخول، لكنه لن يقرأ الطلبات أو يعدّل الوظائف إذا لم تظهر claim داخل ID token.

بعد تعيين claim، يجب تسجيل الخروج والدخول مرة أخرى أو فرض تحديث ID token، ثم اختبار قراءة الطلبات وتغيير حالة طلب.

### 3. نشر قواعد Firestore وStorage

من جهازك، بعد تثبيت Firebase CLI وتسجيل الدخول وربط المشروع:

```bash
firebase use YOUR_FIREBASE_PROJECT_ID
firebase deploy --only firestore:rules,storage
```

يجب اختبار أن الزائر يستطيع إنشاء طلب فقط، وأنه لا يستطيع قراءة مجموعة `applications` أو ملفات Storage، بينما يستطيع المدير القراءة والتحديث.

### 4. نشر الواجهة

تمت إضافة إعدادات النشر إلى المشروع. يمكن اختيار أحد المسارين:

#### Firebase Hosting

```bash
npm ci
npm run lint
npm run build
firebase deploy --only hosting
```

الملف `firebase.json` الآن يوجه Hosting إلى `dist` ويعيد المسارات الداخلية إلى `index.html`، ولذلك ينبغي أن تعمل روابط مثل `/careers` و`/admin` عند فتحها مباشرة.

#### Vercel

اربط مستودع GitHub، واستخدم:

```text
Build command: npm run build
Output directory: dist
Install command: npm ci
```

أضف كل متغيرات `VITE_FIREBASE_*` في Vercel لبيئة Production. ملف `vercel.json` المضاف يعالج deep links الخاصة بتطبيق React SPA.

### 5. اختبار الإنتاج الحقيقي

قبل الإعلان عن الموقع، نفّذ طلباً حقيقياً بملف PDF من الرابط المنشور، ثم تحقق من وجود مستند في `applications`، ووصول المرفق إلى Storage، وعدم ظهور رابط عام للسيرة الذاتية، وظهور الطلب في لوحة المدير.

### 6. إشعارات البريد

الإشعارات **غير مفعلة حالياً**. المشروع لا يحتوي على Cloud Functions أو مزود بريد في `package.json`. المسار الموصى به هو إضافة Cloud Function تعمل عند إنشاء مستند جديد في `applications`، ثم ترسل رسالة إلى بريد الإدارة عبر Resend أو SendGrid أو Mailgun أو Postmark. يجب حفظ مفتاح مزود البريد في Secret خادمي، وليس في `VITE_*`.

يفضل تنفيذ البريد قبل الإطلاق العام إذا كان فريق التوظيف يعتمد عليه؛ وإلا سيصل الطلب إلى Firestore ولوحة الإدارة فقط، وقد لا ينتبه الفريق إلى الطلب الجديد فوراً.

## ترتيب الخطوات التالية

| الأولوية | الإجراء | معيار الإنجاز |
|---|---|---|
| 1 | إعداد مشروع Firebase وWeb App وAuthentication وFirestore وStorage | اتصال حقيقي دون fallback محلي. |
| 2 | إنشاء المدير وتعيين `admin=true` | تسجيل دخول المدير وظهور الطلبات في `/admin`. |
| 3 | نشر القواعد | اختبار رفض القراءة العامة للطلبات والمرفقات. |
| 4 | إضافة متغيرات البيئة في منصة النشر | نجاح build واتصال الموقع بFirebase في Production. |
| 5 | نشر الواجهة | نجاح فتح الصفحة الرئيسية والروابط الداخلية مباشرة. |
| 6 | اختبار طلب حقيقي | المستند والمرفق يظهران للمدير فقط. |
| 7 | إضافة Cloud Function للبريد | وصول إشعار بعد إنشاء طلب جديد مع تسجيل الأخطاء. |
| 8 | تجهيز المحتوى القانوني والتشغيلي | سياسة خصوصية، موافقة على معالجة CV، وبيانات اتصال حقيقية. |

## القرار العملي

إذا كان المقصود بـ **النشر** هو رفع نسخة تجريبية لمراجعتها داخلياً، فالمشروع جاهز الآن بعد إضافة متغيرات البيئة وإعداد Hosting.

إذا كان المقصود بـ **الاستخدام العام واستقبال طلبات المرشحين فعلياً**، فالإجابة هي: **ليس بعد، لكنه قريب من ذلك**. العوائق المتبقية ليست إعادة بناء للواجهة، بل إعداد Firebase الفعلي، الصلاحيات، اختبار الأمان، وإضافة البريد. بعد تنفيذ البنود من 1 إلى 6 يمكن إطلاق نسخة أولى مضبوطة، والبند 7 مهم جداً لتشغيل عملية التوظيف بشكل موثوق.

## مراجع تقنية

[1]: https://firebase.google.com/docs/auth/admin/custom-claims — Firebase custom claims.

[2]: https://firebase.google.com/docs/hosting — Firebase Hosting.

[3]: https://vercel.com/docs/frameworks/frontend/vite — Vite on Vercel.

[4]: https://firebase.google.com/docs/extensions/official/firestore-send-email — Firebase Trigger Email.
